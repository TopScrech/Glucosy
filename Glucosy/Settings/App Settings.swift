import SwiftUI

struct AppSettings: View {
    var body: some View {
        List {
            Text("Settings")
        }
        .navigationTitle("Settings")
    }
}

#Preview {
    AppSettings()
}
